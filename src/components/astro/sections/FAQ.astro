---
/** biome-ignore-all lint/correctness/noUnusedImports: React icons are used in JSX */
/** biome-ignore-all lint/correctness/noUnusedVariables: variables used in Astro components */

import type { FAQProps } from "@/types/app";

interface Props extends FAQProps {}

const { items } = Astro.props;
---

<div class="mb-0">
	<h2 class="mb-6 text-2xl font-semibold text-gray-900 dark:text-white">
		FAQ
	</h2>
	<div class="space-y-4">
		{items.map((item) => (
			<div class="overflow-hidden rounded-xl border border-gray-300 dark:border-white/10 bg-white dark:bg-white/[0.03] shadow-sm">
				<details class="group">
					<summary class="flex cursor-pointer items-center justify-between p-6">
						<h3 class="pr-6 font-medium text-gray-900 dark:text-white">
							{item.question}
						</h3>
						<svg
							class="h-5 w-5 flex-shrink-0 group-open:rotate-180 text-gray-600 dark:text-gray-400"
							fill="none"
							viewBox="0 0 24 24"
							stroke="currentColor"
							aria-label="Toggle answer visibility"
							role="img"
						>
							<path
								stroke-linecap="round"
								stroke-linejoin="round"
								stroke-width={2}
								d="M19 9l-7 7-7-7"
							/>
						</svg>
					</summary>
					<div class="border-t border-gray-200 dark:border-white/5 px-6 pb-6 pt-6">
						<p class="text-gray-600 dark:text-gray-400">{item.answer}</p>
					</div>
				</details>
			</div>
		))}
	</div>
</div>